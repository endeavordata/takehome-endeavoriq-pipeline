name: endeavoriq
version: "1.0"
config-version: 2
profile: endeavoriq

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  endeavoriq:
    staging:
      +materialized: view
    marts:
      +materialized: table
